[project]
name = "endcap-generator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "maciejduk", email = "me@maciejd.uk" }
]
requires-python = ">=3.10,<3.13"
dependencies = [
    "cadquery>=2.7.0",
    "trimesh>=5.1.0",
]

[project.scripts]
endcap-generator = "endcap_generator:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
